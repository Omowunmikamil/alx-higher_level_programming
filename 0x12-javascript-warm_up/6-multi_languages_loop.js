#!/usr/bin/node
// Print basic JavaScript

const array = ['C is fun', 'Python is cool', 'JavaScript is amaizing'];

for (let index = 0, len = array.length; index < len; index++) {
  console.log(array[index]);
}
